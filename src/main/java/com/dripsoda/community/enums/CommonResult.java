package com.dripsoda.community.enums;

import com.dripsoda.community.interfaces.IResult;

public enum CommonResult implements IResult {

    SUCCESS,
    FAILURE,
    FAILURE_EXPIRED,
    DUPLICATE
}
